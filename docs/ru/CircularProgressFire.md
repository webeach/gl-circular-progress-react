# CircularProgressFire

## Описание

`CircularProgressFire` — это React-компонент, обертка над библиотекой `gl-circular-progress`, для создания кругового прогресс-бара с эффектом **огня** на WebGL.
Позволяет гибко настраивать цвета, скорость и интенсивность пламени через пропсы.

Эффект рендерится на элементе `<canvas>`, который управляется компонентом.

[👀 Смотреть Демо](https://webeach.github.io/gl-circular-progress/CircularProgressFire.html)

---

## Сигнатура

```tsx
<CircularProgressFire options={options} aria-label="Загрузка..." />
```

---

## Примеры

### Базовое использование

```tsx
import { CircularProgressFire } from '@webeach/gl-circular-progress-react';

function App() {
  return (
    <div style={{ width: 200, height: 200 }}>
      <CircularProgressFire
        aria-label="Огненный прогресс"
        options={{
          colors: [0xff5a00, 0xff9a00, 0xffce00], // Цвета огня
          intensity: 1.0,
          progress: 0.75, // Начальное значение
          speed: 1.5,
          thickness: 20,
        }}
      />
    </div>
  );
}
```

### Динамическое обновление

Обновляйте проп `options` для изменения эффекта "на лету". Компонент эффективно обрабатывает обновления.

```tsx
import { useState } from 'react';
import { CircularProgressFire } from '@webeach/gl-circular-progress-react';

function DynamicExample() {
  const [progress, setProgress] = useState(0);

  return (
    <CircularProgressFire
      aria-label="Динамический огонь"
      options={{
        colors: [0xff5a00, 0xff9a00],
        progress: progress, // Динамическое значение
        speed: 1.5,
        thickness: 20,
      }}
    />
  );
}
```

### С дочерними элементами (Контент по центру)

Вы можете разместить контент внутри прогресс-бара. Используйте `contentLayer`, чтобы управлять тем, находится ли он за или перед эффектами.

```tsx
<CircularProgressFire
  aria-label="Прогресс с текстом"
  options={{ ...options, progress: 0.5 }}
>
  <span style={{ color: 'white', fontSize: '2rem' }}>50%</span>
</CircularProgressFire>
```

---

## API

### Пропсы (Props)

| Проп           | Тип                            | По умолчанию    | Описание                                           |
|----------------|--------------------------------|-----------------|----------------------------------------------------|
| `options`      | `CircularProgressFireOptions`  | **Обязательно** | Объект конфигурации для эффекта.                   |
| `aria-label`   | `string`                       | **Обязательно** | Доступная метка для прогресс-бара.                 |
| `tagName`      | `keyof JSX.IntrinsicElements`  | `'div'`         | HTML-тег для контейнера обертки.                   |
| `contentFit`   | `'contain' \| 'cover'`         | `undefined`     | Как контент вписывается в контейнер.               |
| `contentLayer` | `'background' \| 'foreground'` | `'background'`  | Z-порядок дочерних элементов относительно канваса. |

Компонент также принимает все стандартные HTML-атрибуты и обработчики событий, поддерживаемые указанным `tagName` (по умолчанию `'div'`), а также `ref`.

### Опции (`CircularProgressFireOptions`)

Объект настроек, передаваемый в проп `options`.

| Свойство     | Тип        | По умолчанию    | Описание                                                                          |
|--------------|------------|-----------------|-----------------------------------------------------------------------------------|
| `colors`     | `number[]` | **Обязательно** | Массив цветов в формате HEX (например, `0xff5a00`). Используется для градиента.   |
| `intensity`  | `number`   | `1.0`           | Интенсивность эффекта огня.                                                       |
| `progress`   | `number`   | `0`             | Начальное значение прогресса (от 0.0 до 1.0).                                     |
| `reversed`   | `boolean`  | `false`         | Направление заполнения. Если `true`, прогресс заполняется против часовой стрелки. |
| `speed`      | `number`   | `1.0`           | Скорость анимации пламени.                                                        |
| `startAngle` | `number`   | `0`             | Начальный угол прогресса в градусах.                                              |
| `thickness`  | `number`   | `10`            | Толщина кольца прогресса в пикселях.                                              |

---

## Типизация

```ts
import type { CircularProgressFireProps, CircularProgressFireOptions } from '@webeach/gl-circular-progress-react';
```

---

## Смотрите также

- [CircularProgressLiquid](./CircularProgressLiquid.md) — эффект жидкости.

