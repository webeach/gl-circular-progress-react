# CircularProgressLiquid

## Описание

`CircularProgressLiquid` — это React-компонент, обертка над библиотекой `gl-circular-progress`, для создания кругового прогресс-бара с эффектом **жидкости** (metaballs) на WebGL.
Позволяет создавать плавные анимации с настройкой объемного искажения (эффект линзы) через пропсы.

Эффект рендерится на элементе `<canvas>`, который управляется компонентом.

[👀 Смотреть Демо](https://webeach.github.io/gl-circular-progress/CircularProgressLiquid.html)

---

## Сигнатура

```tsx
<CircularProgressLiquid options={options} aria-label="Загрузка..." />
```

---

## Примеры

### Базовое использование

```tsx
import { CircularProgressLiquid } from '@webeach/gl-circular-progress-react';

function App() {
  return (
    <div style={{ width: 200, height: 200 }}>
      <CircularProgressLiquid
        aria-label="Жидкий прогресс"
        options={{
          colors: [0x00c6ff, 0x0072ff], // Цвета жидкости
          progress: 0.6, // Начальное значение
          speed: 0.5,
          startAngle: -90, // Начать слева
          thickness: 30,
          volume: 0.5, // Эффект объема/линзы
        }}
      />
    </div>
  );
}
```

### Динамическое обновление

Обновляйте проп `options` для изменения эффекта "на лету". Компонент эффективно обрабатывает обновления.

```tsx
import { useState } from 'react';
import { CircularProgressLiquid } from '@webeach/gl-circular-progress-react';

function DynamicExample() {
  const [progress, setProgress] = useState(0);

  return (
    <CircularProgressLiquid
      aria-label="Динамическая жидкость"
      options={{
        colors: [0x00c6ff, 0x0072ff],
        progress: progress, // Динамическое значение
        speed: 1.2, // Ускорить течение
        volume: 0.8, // Увеличить эффект линзы
        reversed: true, // Изменить направление
      }}
    />
  );
}
```

---

## API

### Пропсы (Props)

| Проп           | Тип                             | По умолчанию    | Описание                                           |
|----------------|---------------------------------|-----------------|----------------------------------------------------|
| `options`      | `CircularProgressLiquidOptions` | **Обязательно** | Объект конфигурации для эффекта.                   |
| `aria-label`   | `string`                        | **Обязательно** | Доступная метка для прогресс-бара.                 |
| `tagName`      | `keyof JSX.IntrinsicElements`   | `'div'`         | HTML-тег для контейнера обертки.                   |
| `contentFit`   | `'contain' \| 'cover'`          | `undefined`     | Как контент вписывается в контейнер.               |
| `contentLayer` | `'background' \| 'foreground'`  | `'background'`  | Z-порядок дочерних элементов относительно канваса. |

Компонент также принимает все стандартные HTML-атрибуты и обработчики событий, поддерживаемые указанным `tagName` (по умолчанию `'div'`), а также `ref`.

### Опции (`CircularProgressLiquidOptions`)

Объект настроек, передаваемый в проп `options`.

| Свойство     | Тип        | По умолчанию    | Описание                                                                          |
|--------------|------------|-----------------|-----------------------------------------------------------------------------------|
| `colors`     | `number[]` | **Обязательно** | Массив цветов в формате HEX. Используется для градиента жидкости.                 |
| `progress`   | `number`   | `0`             | Начальное значение прогресса (от 0.0 до 1.0).                                     |
| `reversed`   | `boolean`  | `false`         | Направление заполнения. Если `true`, прогресс заполняется против часовой стрелки. |
| `speed`      | `number`   | `1.0`           | Скорость анимации волн жидкости.                                                  |
| `startAngle` | `number`   | `0`             | Начальный угол прогресса в градусах.                                              |
| `thickness`  | `number`   | `10`            | Толщина кольца прогресса в пикселях.                                              |
| `volume`     | `number`   | `0`             | Эффект объема (линзы/3D). Значение от `0.0` (плоский) до `1.0` (сильный эффект).  |

---

## Типизация

```ts
import type { CircularProgressLiquidProps, CircularProgressLiquidOptions } from '@webeach/gl-circular-progress-react';
```

---

## Смотрите также

- [CircularProgressFire](./CircularProgressFire.md) — эффект огня.

